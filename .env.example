# Host folders mounted into the container (used by compose.yaml).
# Put your legacy .xls files in ./docs; converted .xlsx files appear in ./out.
XLS_SRC_DIR=./docs
XLS_OUT_DIR=./out

# Parallel LibreOffice workers (default: 4).
WORKERS=4
